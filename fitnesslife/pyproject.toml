[tool.poetry]
name = "fitnesslife"
version = "0.1.0"
description = ""
authors = ["pavelzurnin <yuujuukin@icloud.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
django = "^4.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
